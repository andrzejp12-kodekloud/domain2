name: Deploy All

on:
    workflow_dispatch:

jobs:
    deploy:
        name: Deploy
        strategy:
            matrix:
                env: ["test", "prod"]
        uses: ./.github/workflows/dep.yml
        secrets: inherit
        permissions:
            actions: read
            contents: write
        with:
            env: ${{ matrix.env }}
